fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.42
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32

# --- TIFF-stack -> OME-Zarr conversion (backend/app/helpers/volume.py) ---
tifffile==2026.8.23
numpy==2.5.3
# ome-zarr-py writes/reads via the zarr-python v2 API; pin below v3 until it
# adds v3 support, or `write_image()` will fail on import/attribute errors.
zarr==3.3.0
ome-zarr==0.19.1

# --- Mesh and point cloud processing (backend/app/helpers/mesh.py) ---
trimesh==5.1.0
